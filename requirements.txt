# pip fallback (prefer `uv sync`). CUDA 12.4 build for the cluster A100 nodes.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
numpy
torch-fidelity>=0.3
wandb>=0.16
